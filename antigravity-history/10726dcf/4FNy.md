# Mobile Migration Task List (React Native / Expo)

- [ ] Initialize a new Expo project alongside the existing Next.js project.
- [ ] Configure Supabase client for React Native (using `react-native-url-polyfill`).
- [ ] Implement Expo Router for navigation (Stack and Tabs).
- [ ] Rebuild the Authentication flow (Login/Register) using Native UI components.
- [ ] Rebuild the core Player flows (Dashboard, Court Search, Booking, Profile).
- [ ] Rebuild the Admin/Owner flows (Availability Grid, Booking Management).
- [ ] Migrate server-side Next.js API Routes (`src/app/api/*`) to Supabase Edge Functions.
- [ ] Set up Expo Application Services (EAS) for building the Android APK and iOS IPA.
