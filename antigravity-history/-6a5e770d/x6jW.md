# Phase 12: Sync, Upload, and Admin Refinement

## Sync & Data Persistence
- [ ] **My Classes Sync**: Ensure cancelled classes don't show as "próxima clase".
- [ ] **Admin Refresh**: Classes created in `create.tsx` should appear immediately in `dashboard.tsx`.
- [ ] **Schedule Persistence**: Fix blocks and class creation not saving in the correct slot.

## Bug Fixes
- [ ] **Receipt Upload**: Resolve "Network request failed" in `payments.tsx`.
- [ ] **Table Name**: Fix typo `app_setting` -> `app_settings` in `config.tsx`.
- [ ] **Student Search**: Fix "Javier Aravena" appearing in search results.

## UI Improvements
- [ ] **Blocked Label**: Only show "Bloqueada", remove struck-through class name.
- [ ] **Admin Transitions**: Remove white flicker in `(admin)/_layout.tsx`.
- [ ] **Admin Bottom Bar**: Responsive layout to show all actions without scrolling.
